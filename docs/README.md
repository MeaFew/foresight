# Documentation

This directory hosts extended documentation for the foresight project.

## Contents

- **Architecture & design notes** — add deeper write-ups of the feature
  engineering, time-aware validation split, and model comparison methodology.
- **Experiment logs** — record hyperparameter sweeps and ablation results.

The project README (`../README.md`) covers quick start, the tech-stack table,
and the headline benchmark results. Place longer-form material here.

> Project reports generated by the pipeline are written to `../reports/`
> (e.g. `model_results.json`, residual plots).
